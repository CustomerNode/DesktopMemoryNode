# shared/

Cross-platform pieces used by both the Windows and Linux Mint agents.

## Planned contents

- `memorybox-client/` — client library/CLI for talking to the cloud memorybox
- `manifest/` — snapshot manifest schema (what was backed up, when, integrity hashes)
- `config-schema/` — common config validation
